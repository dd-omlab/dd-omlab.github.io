% proclinec.m: read ONE LINE of text and return a list of words that % appeared in the in that line. A 'word' is text bordered by by a whitespace% character. The default ws_chars are: space,tab,LF,CR%% Usage: [word_list, numwords] = proclinec(input_text,ws_char)%   ws_char: cell list of word sep chars. Leave empty to use defaults.  %   word_list: cell array of words on that line.%   % Written by:  Jonathan Jacobs%              July 1997  (last mod: 07/02/97)function [word_list, numwords] = proclinec(input_text,ws_char)if nargin==0   help proclinec   returnendword_list=[];numwords=0;if isempty(input_text); return; endif nargin<2   ws_char={char(9),char(32),char(13),char(10)}; % whitespace: tab,space,cr,lfendcount=1;word_list=cell(100,1);while( ~all(isspace(input_text)) )   [temp, input_text] = strtok(input_text,ws_char); %#ok<STTOK>   word_list{count}=temp;   count=count+1;endnumwords  = count-1;word_list = word_list(1:numwords);